/* eslint-disable no-undef */
test('renders plans title', () => {
    // Prueba 1: renderizar el título de los planes
 });

test('debe devolver un string vacío si recibe un string vacío', () => {
   expect('').toBe('');
});
